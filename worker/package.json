{
  "name": "triple-yahtzee-report-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker that proxies issue reports to GitHub",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
